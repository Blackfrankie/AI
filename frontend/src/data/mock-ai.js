// Mock AI responses for all AI modules
// This simulates real AI API responses and will be replaced with actual AI integrations

export const mockWebsiteAI = {
  generateWebsite: (description, type) => ({
    preview: `
      <div style="font-family: Arial, sans-serif; max-width: 800px; margin: 0 auto;">
        <header style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 2rem; text-align: center; border-radius: 10px; margin-bottom: 2rem;">
          <h1 style="margin: 0; font-size: 2.5rem;">Welcome to ${type || 'Your'} Website</h1>
          <p style="margin: 0.5rem 0 0 0; opacity: 0.9;">Generated by AI based on your description</p>
        </header>
        
        <section style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem; margin-bottom: 2rem;">
          <div style="background: #f8f9fa; padding: 1.5rem; border-radius: 10px; border-left: 4px solid #667eea;">
            <h3 style="color: #333; margin-top: 0;">Feature One</h3>
            <p style="color: #666;">AI-generated content based on your requirements. Modern, responsive, and optimized.</p>
          </div>
          <div style="background: #f8f9fa; padding: 1.5rem; border-radius: 10px; border-left: 4px solid #764ba2;">
            <h3 style="color: #333; margin-top: 0;">Feature Two</h3>
            <p style="color: #666;">Professional design with clean layout and user-friendly interface elements.</p>
          </div>
        </section>
        
        <footer style="text-align: center; padding: 1rem; background: #333; color: white; border-radius: 10px;">
          <p style="margin: 0;">© 2024 AI Generated Website - Ready to deploy!</p>
        </footer>
      </div>
    `,
    stats: {
      files: 5,
      components: 8,
      responsive: '✓',
      seo: '95/100'
    },
    features: [
      'Responsive Design', 'SEO Optimized', 'Modern UI/UX', 'Cross-browser Compatible',
      'Mobile-first', 'Fast Loading', 'Accessible', 'Clean Code'
    ]
  })
};

export const mockGameAI = {
  generateGame: (idea, type, platform) => ({
    title: `${type?.charAt(0).toUpperCase() + type?.slice(1) || 'Epic'} Adventure`,
    description: `An AI-generated ${type || 'adventure'} game based on your concept. Features engaging gameplay, smooth mechanics, and immersive experience.`,
    icon: '🎮',
    stats: {
      levels: Math.floor(Math.random() * 20) + 5,
      assets: Math.floor(Math.random() * 50) + 20,
      codeLines: Math.floor(Math.random() * 5000) + 1000,
      playtime: `${Math.floor(Math.random() * 10) + 2}h`
    },
    features: [
      'Smooth Gameplay', 'Dynamic Difficulty', 'Achievement System', 'Save/Load Game',
      'Multiple Levels', 'Sound Effects', 'Particle Effects', 'Leaderboard'
    ],
    techStack: [
      platform === 'web' ? 'HTML5 Canvas' : 'Native Code',
      'JavaScript', 'WebGL', 'Audio API', 'LocalStorage'
    ]
  })
};

export const mockContentAI = {
  generateContent: (topic, type, tone, length) => {
    const wordCount = length === 'short' ? 350 : length === 'long' ? 1200 : 650;
    const sentences = Math.floor(wordCount / 15);
    
    return {
      content: `
        <h2>The Future of ${topic}</h2>
        <p>In today's rapidly evolving digital landscape, ${topic} has become increasingly important for businesses and individuals alike. This comprehensive analysis explores the various aspects and implications of this fascinating subject.</p>
        
        <h3>Key Benefits and Advantages</h3>
        <p>The implementation of ${topic} offers numerous advantages including improved efficiency, enhanced user experience, and cost-effective solutions. Organizations that embrace these innovations often see significant improvements in their operations.</p>
        
        <h3>Current Trends and Developments</h3>
        <p>Recent developments in ${topic} have shown remarkable progress. Industry leaders are investing heavily in research and development, leading to breakthrough innovations that are reshaping the entire sector.</p>
        
        <h3>Looking Forward</h3>
        <p>As we move into the future, ${topic} will continue to play a crucial role in shaping our digital world. The potential applications are limitless, and we're only beginning to scratch the surface of what's possible.</p>
        
        <p><strong>Conclusion:</strong> The journey ahead is exciting, and those who adapt early will be best positioned to take advantage of these emerging opportunities.</p>
      `,
      stats: {
        words: wordCount,
        sentences: sentences,
        readTime: `${Math.ceil(wordCount / 200)}min`,
        seoScore: '88/100'
      },
      features: [
        'SEO Optimized', 'Engaging Headlines', 'Clear Structure', 'Call-to-Action',
        'Keyword Rich', 'Scannable Format', 'Professional Tone'
      ]
    };
  }
};

export const mockImageAI = {
  generateImages: (prompt, style, size, quality) => {
    const imageCount = 4;
    const images = [];
    
    for (let i = 0; i < imageCount; i++) {
      images.push({
        url: `https://picsum.photos/400/400?random=${Math.random()}`,
        description: `AI generated ${style} image based on: "${prompt}"`,
        style: style,
        resolution: size,
        fileSize: `${Math.floor(Math.random() * 5) + 1}MB`
      });
    }
    
    return images;
  }
};

export const mockCodeAI = {
  generateCode: (request, language, taskType) => ({
    language: language.toUpperCase(),
    code: language === 'javascript' ? `
// ${taskType} - Generated by AI
function ${taskType?.replace(' ', '') || 'solution'}() {
  // AI-generated code based on: ${request}
  
  const result = {
    status: 'success',
    data: [],
    timestamp: new Date().toISOString()
  };
  
  // Main logic implementation
  try {
    // Process the request
    console.log('Processing request:', '${request}');
    
    // Return structured response
    return result;
  } catch (error) {
    console.error('Error:', error);
    return { status: 'error', message: error.message };
  }
}

// Usage example
const output = ${taskType?.replace(' ', '') || 'solution'}();
console.log(output);
    ` : `
# ${taskType} - Generated by AI
def ${taskType?.replace(' ', '_').toLowerCase() || 'solution'}():
    """
    AI-generated code based on: ${request}
    """
    
    result = {
        'status': 'success',
        'data': [],
        'timestamp': str(datetime.now())
    }
    
    try:
        # Main logic implementation
        print(f"Processing request: {request}")
        
        # Return structured response
        return result
        
    except Exception as e:
        print(f"Error: {e}")
        return {'status': 'error', 'message': str(e)}

# Usage example
output = ${taskType?.replace(' ', '_').toLowerCase() || 'solution'}()
print(output)
    `,
    stats: {
      lines: Math.floor(Math.random() * 50) + 20,
      functions: Math.floor(Math.random() * 5) + 1,
      complexity: 'Medium',
      quality: '94%'
    },
    features: [
      'Error Handling', 'Documentation', 'Clean Code', 'Best Practices',
      'Optimized Performance', 'Type Safety', 'Modular Design'
    ],
    explanation: `This ${language} code implements ${taskType?.toLowerCase() || 'the requested functionality'} based on your requirements. It includes proper error handling, clear variable names, and follows best practices for maintainable code.`
  })
};

export const mockDataAI = {
  analyzeData: (data, analysisType, source) => ({
    confidence: Math.floor(Math.random() * 20) + 80,
    metrics: {
      dataPoints: Math.floor(Math.random() * 10000) + 500,
      accuracy: `${Math.floor(Math.random() * 15) + 85}%`,
      patterns: Math.floor(Math.random() * 10) + 3,
      insights: Math.floor(Math.random() * 8) + 5
    },
    chart: {
      type: 'Interactive Dashboard',
      icon: '📊',
      description: 'Multi-dimensional visualization showing trends, patterns, and correlations in your data.'
    },
    insights: [
      {
        icon: '📈',
        title: 'Significant Growth Trend',
        description: 'Data shows a 23% upward trend in the analyzed metrics over the examined period.',
        impact: 'High'
      },
      {
        icon: '🔍',
        title: 'Pattern Recognition',
        description: 'Identified recurring patterns that occur every 7-day cycle with 89% consistency.',
        impact: 'Medium'
      },
      {
        icon: '⚡',
        title: 'Anomaly Detection',
        description: 'Found 3 significant outliers that may require attention or further investigation.',
        impact: 'Medium'
      }
    ],
    recommendations: [
      'Implement predictive modeling', 'Set up automated monitoring', 'Create data pipeline',
      'Schedule regular analysis', 'Deploy ML algorithms'
    ]
  })
};

export const mockCreativeAI = {
  createContent: (prompt, type, style, mood) => {
    const contents = {
      poem: `In digital realms where dreams take flight,
AI whispers through the night,
Creating worlds with lines of code,
Along imagination's winding road.

Binary stars in cyber space,
Dance with algorithmic grace,
While pixels paint what hearts conceive,
In this world we make believe.

The future flows through circuits bright,
Where human dreams and data unite,
In harmony of mind and machine,
The most beautiful sight ever seen.`,
      
      story: `The old lighthouse keeper had seen many storms, but none quite like this. As the waves crashed against the rocky shore, she noticed something unusual in the water—a soft, ethereal glow that seemed to pulse with the rhythm of the tide.

"Strange," she murmured, adjusting her weathered coat against the wind. For forty years, she had watched over this stretch of coast, guiding ships safely home. But tonight felt different.

The glow grew brighter, revealing what appeared to be a message written in the foam itself—letters that formed and dissolved with each wave. She leaned forward, squinting through the storm, and gasped as she finally made out the words: "Thank you for keeping the light burning."

In that moment, she understood that every ship she had guided, every life she had touched, had become part of something greater—a network of gratitude spanning decades and oceans.`,
      
      song: `[Verse 1]
Walking down this neon street tonight
City lights are burning bright
Dreams are calling out my name
Nothing ever feels the same

[Chorus]
We're reaching for tomorrow
Through the joy and through the sorrow
Every step we take today
Lights the path along the way

[Verse 2]
Stars are dancing overhead
Painting silver, gold, and red
Music flowing through the air
Magic moments everywhere

[Chorus]
We're reaching for tomorrow
Through the joy and through the sorrow
Every step we take today
Lights the path along the way`
    };

    const selectedContent = contents[type] || contents.poem;
    
    return {
      title: `${style.charAt(0).toUpperCase() + style.slice(1)} ${type?.charAt(0).toUpperCase() + type?.slice(1) || 'Creation'}`,
      type: type || 'poem',
      style: style,
      content: selectedContent,
      stats: {
        words: selectedContent.split(' ').length,
        verses: type === 'poem' ? selectedContent.split('\n\n').length : undefined,
        paragraphs: type !== 'poem' ? selectedContent.split('\n\n').length : undefined,
        readTime: `${Math.ceil(selectedContent.split(' ').length / 200)}min`,
        creativity: `${Math.floor(Math.random() * 20) + 80}%`
      },
      elements: [
        'Metaphor', 'Imagery', 'Rhythm', 'Emotional Depth',
        'Symbolism', 'Flow', 'Vivid Language'
      ],
      analysis: `This ${type} demonstrates strong ${style} influences with ${mood} undertones. The work successfully balances creative expression with structural integrity, creating an engaging piece that resonates with the intended audience.`
    };
  }
};